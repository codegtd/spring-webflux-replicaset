version: "3.4"

services:
  api-test:
    image: mongo:4.4.4
    ports:
      - 27017:27017
    restart: always
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
      - MONGO_AUTH_DB=admin
#      - MONGO_PORT=27017
#      - MONGO_DB=test-db
#      - MONGO_HOST=localhost