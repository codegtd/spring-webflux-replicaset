spring:
  profiles:
    # active: test-comp
    group:
      test-std: log, standalone # Docker-Compose: compose-standalone.yml
      test-rs-node3: log, rs-node3 # Docker-Compose: compose-rs-node3.yml
      test-cmp: log, tc-compose # Testcontainer-ComposeModule - DOES NOT ALLOW Transactions
      test-cont-tr: log, tc-container # Testcontainer-ContainerModule - ALLOW Transactions