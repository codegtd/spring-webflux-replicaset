spring:
  profiles:
    # active: test-comp
    group:
      # ALLOW Transactions - REPLICASET
      test-rs: log, replicaset # Docker-Compose: compose-replicaset.yml
      test-tc-rs-tr: log, tc-rs-transaction # Testcontainer-ContainerModule

      # NOT ALLOW Transactions - STAND-ALONE
      test-std: log, standalone # Docker-Compose: compose-standalone.yml
      test-tc-comp: log, tc-compose # Testcontainer-ComposeModule